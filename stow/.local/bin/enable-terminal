#!/usr/bin/env sh

set -e

brew install \
    bash bash-completion \
    zsh zsh-completions \
    curl lynx wget whois newsboat \
    vim words aspell git stow direnv \
    tmux ranger trash tree pstree \
    atool socat wget fswatch watch \
    coreutils moreutils dateutils findutils renameutils
